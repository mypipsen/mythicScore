# mythicScore

Track and visualize any character's mythic+ score progression over time. Built with Vite, TypeScript, and Recharts.

## Local Setup
```bash
npm install
npm run dev
```

## Deployment
Push the repository to GitHub and import it into **Vercel**. The included `vercel.json` file automatically handles Raider.io API proxying in production.
